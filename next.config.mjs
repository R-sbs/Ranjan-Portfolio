/** @type {import('next').NextConfig} */
const nextConfig = {
    output: "export", 
    basePath: "/Ranjan-Portfolio",
    images: {
      unoptimized: true,
    },
  };
  
  export default nextConfig;
  